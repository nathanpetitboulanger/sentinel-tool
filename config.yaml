# Satellite indices to calculate
indices:
  - NDVI
  - NDWI

# STAC API configuration
stac_api_url: "https://earth-search.aws.element84.com/v1"
collection: "sentinel-2-l2a"

# Dask configuration
dask:
  threads_per_worker: 4
  n_workers: 4
